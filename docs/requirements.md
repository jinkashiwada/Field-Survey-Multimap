# 要件

## 目的と利用者

水害時の痕跡調査、事前調査、現地踏査を行う研究者・学生・技術者が、公式地理空間情報を一般的なPC・タブレット・スマートフォンのブラウザーで比較できるようにする。

## Version 0.1の機能要件

- 1・2・4個のOpenLayers Mapが同じViewを共有し、中心、解像度、ズーム、回転を同期する。
- 携帯電話では最大2画面とし、4画面指定は先頭2画面へ縮退する。
- 各ペインは背景地図、複数の重畳レイヤー、透明度、凡例を独立して保持する。
- レイヤー設定パネルは画面間で同一の開閉規則を持ち、同時に複数を開かない。
- 必須13レイヤーに加え、解析用色別標高と試験公開の道路・鉄道・河川・等高線を内蔵する。4つの参照レイヤーは初期表示、5プリセット、画面追加時に有効とし、常時確認可能な出典を提供する。
- 全国最新写真は同一公式URLの衛星モザイクへ接続し、低ズームでも写真系背景を継続する。
- 解析用色別標高は手動レンジ、常設の手動推定、自動推定に対応する。自動でもmoveend後に16地点だけを標本抽出し、地図移動中の連続再配色はしない。
- 河川中心線はZL6以上で表示し、低ズームの大・中河川を連続ガイドとして保持しつつ、詳細線と水域を重ねる。収録されない管理者区分を推測しない。
- 河川名ホバー、地図の右クリック／長押し地点操作、座標・保存ピン・読込みGISの端末内検索を提供する。
- ボタン操作時だけ現在地を取得し、マーカーと精度円を全画面へ表示する。
- 現在の地図中心を引き継ぎ、国土交通省の公式マップで水位計・危機管理型水位計・河川監視カメラを確認できる。施設位置や観測値は本アプリで取得しない。
- 中心の緯度・経度・ズームと、取得可能な国土地理院DEM標高を表示する。
- 調査ピンをlocalStorageへ保存し、CRUDとKML・GeoJSON出力に対応する。
- EPSG:4326のローカルKML・GeoJSONを3857へ変換して表示し、4326で出力する。
- 通常の表示URLは地図設定だけを含む。明示的な「このピンを共有」では選択した1件の名称・種別・座標・標高・DEM種別、任意のメモをhashへ追加する。共有時は全hashをブラウザー内でgzip＋base64url圧縮し、外部短縮サービスへ情報を送らない。従来の非圧縮URLも受け付ける。現在地マーカー・読込みGIS・他の保存ピンは含めない。
- 共有先は対象ピンを中心に表示し、受信ピンは全Mapで一時表示する。明示保存で新規IDを発行し、既存ピンを上書きしない。不正・過大な共有ピンでも地図を操作できる。
- 一部の外部タイル・DEMが失敗しても、他の地図とローカルデータ操作を継続できる。

## 非機能要件

- GitHub Pages上の静的ファイルだけで完結し、サブパスで動作する。
- 主要操作はキーボード利用可能、明確なフォーカス表示、可能な限り44px以上とする。
- 移動中のReact更新を避け、moveendとデバウンスで中心・URL・DEMを更新する。
- 可変標高はタイル受信時だけCanvas再配色し、等高線は端末生成せず既成ベクトルを既定オフで使う。
- `npm run check` と外部配信に依存しないPlaywright E2Eを品質ゲートとする。

## 禁止事項

独自バックエンド、データベース、認証、秘密情報、アクセス解析、広告、大量タイル保存、非公開API、スクレイピング、無制限CORSプロキシを追加しない。全国河川データ、KMZ、外部URLからのGIS読込みもVersion 0.1には含めない。
